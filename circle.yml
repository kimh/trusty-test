dependencies:
  override:
    - exit 0

test:
  override:
    - exit 0

notify:
  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
    - url: http://1eddad6c.ngrok.io
