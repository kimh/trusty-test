machine:
  services:
    - rabbitmq-server
    - memcached
    - beanstalkd

  node:
    version:
      v4.1.2

dependencies:
  override:
    - exit 0

test:
  override:
    - ps ax | grep -i memcached

