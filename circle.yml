machine:
  pre:
    - sudo apt-get update; USE_PRECOMPILE=true sudo -E circleci-install ruby 2.4.0
  ruby:
    version: 2.4.0

dependencies:
  override:
    - exit 0

test:
  override:
    - ruby --version

