dependencies:

  override:
    - exit 0

test:
  override:
    - wget -q https://ftp.postgresql.org/pub/source/v9.5.4/postgresql-9.5.4.tar.gz
    - tar zxvf postgresql-9.5.4.tar.gz 
    - cd postgresql-9.5.4
    - ./configure
    - make
