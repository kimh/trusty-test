machine:
  pre:
    #- sudo mv /usr/lib/postgresql/9.6 /usr/lib/postgresql/_9.6  
    #- sudo mv /etc/postgresql/9.6 /etc/postgresql/_9.6
    #- sudo mv /usr/lib/postgresql/9.5 /usr/lib/postgresql/_9.5  
    #- sudo mv /etc/postgresql/9.5 /etc/postgresql/_9.5
    - sudo mv /usr/lib/postgresql/9.6 /usr/lib/postgresql/_9.6  
    - sudo mv /etc/postgresql/9.6 /etc/postgresql/_9.6

  override:
    - exit 0

test:
  override:
    - ps ax | grep postgresql

