machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circle-env.sh | bash

dependencies:
  pre:
    - echo "this is from kirk config"
  post:
    - echo "this is from kirk config"
  override:
    - exit 0

test:
  pre:
    - echo "this is pre test from kirk config"
  post:
    - echo "this is post test from kirk config"
  override:
    - echo "this is override test from kirk config"
