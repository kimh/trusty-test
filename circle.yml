machine:
  pre:
    - mv $ANDROID_HOME /tmp/android-sdk-linux
    - sed -i 's|/usr/local/android-sdk-linux|/tmp/android-sdk-linux|g' /home/ubuntu/.circlerc

dependencies:
  override:
    - exit 0

test:
  override:
    - which android
    - android -help

