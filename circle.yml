machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circle-env.sh | bash

dependencies:
  override:
    - sudo apt-get install jq

test:
  override:
    - bash pkg-versions.sh > $CIRCLE_ARTIFACTS/versions.json

