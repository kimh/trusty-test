machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circle-env.sh | bash
  ruby:
    version: system

dependencies:
  override:
    - ruby --version

test:
  override:
    - exit 0

