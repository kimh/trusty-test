machine:
  override:
    - echo $CIRCLE_NODE_INDEX

database:
  override:
    - echo $CIRCLE_NODE_INDEX

dependencies:
  override:
    - echo $CIRCLE_NODE_INDEX

test:
  override:
    - echo $CIRCLE_NODE_INDEX

