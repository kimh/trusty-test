machine: 
  java: 
    version: openjdk7

  environment:
    PATH: $(echo $PATH | sed 's|/usr/lib/jvm/jdk1.8.0/bin:||')

dependencies:
  override:
    - exit 0

test:
  override:
    - java -version

