machine:
  pre:
    - curl -L https://raw.githubusercontent.com/kimh/docker-cache-shim/master/install.sh | bash

dependencies:
  override:
    - docker-cache-shim --help

test:
  override:
    - exit 0

